<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.go.babelprov.favoritecatalogue">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="id.go.babelprov.go.id.moviecatalogues5.READ_DATABASE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_mc_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.dark"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.FullPosterActivity"></activity>
        <activity android:name=".view.DetailTvshowActivity" />
        <activity android:name=".view.DetailMovieActivity" />
        <activity android:name=".view.AboutActivity" />
        <activity android:name=".view.ChangeLanguageActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>